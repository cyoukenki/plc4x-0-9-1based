/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.ads.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.ads.readwrite.*;
import org.apache.plc4x.java.ads.readwrite.io.*;
import org.apache.plc4x.java.ads.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class AdsWriteRequestIO implements MessageIO<AdsWriteRequest, AdsWriteRequest> {

    private static final Logger LOGGER = LoggerFactory.getLogger(AdsWriteRequestIO.class);

    @Override
    public AdsWriteRequest parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return (AdsWriteRequest) new AdsDataIO().parse(readBuffer, args);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, AdsWriteRequest value, Object... args) throws ParseException {
        new AdsDataIO().serialize(writeBuffer, value, args);
    }

    public static AdsWriteRequestBuilder staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("AdsWriteRequest");
        int startPos = readBuffer.getPos();
        int curPos;


        // Simple Field (indexGroup)
long indexGroup = readBuffer.readUnsignedLong("indexGroup", 32) ;

        // Simple Field (indexOffset)
long indexOffset = readBuffer.readUnsignedLong("indexOffset", 32) ;
        // Implicit Field (length) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        long length = readBuffer.readUnsignedLong("length", 32);
        // Array field (data)
        readBuffer.pullContext("data", WithReaderWriterArgs.WithRenderAsList(true));
        // Count array
        if(length > Integer.MAX_VALUE) {
            throw new ParseException("Array count of " + (length) + " exceeds the maximum allowed count of " + Integer.MAX_VALUE);
        }
        byte[] data;
        {
            int itemCount = Math.max(0, (int) length);
            data = new byte[itemCount];
            for(int curItem = 0; curItem < itemCount; curItem++) {
                
data[curItem] = readBuffer.readSignedByte("", 8) ;            }
        }
            readBuffer.closeContext("data", WithReaderWriterArgs.WithRenderAsList(true));

        readBuffer.closeContext("AdsWriteRequest");
        // Create the instance
        return new AdsWriteRequestBuilder(indexGroup, indexOffset, data);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, AdsWriteRequest _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("AdsWriteRequest");

        // Simple Field (indexGroup)
        long indexGroup = (long) _value.getIndexGroup();
        writeBuffer.writeUnsignedLong("indexGroup", 32, ((Number) (indexGroup)).longValue());

        // Simple Field (indexOffset)
        long indexOffset = (long) _value.getIndexOffset();
        writeBuffer.writeUnsignedLong("indexOffset", 32, ((Number) (indexOffset)).longValue());

        // Implicit Field (length) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        long length = (long) (COUNT(_value.getData()));
        writeBuffer.writeUnsignedLong("length", 32, ((Number) (length)).longValue());

        // Array Field (data)
        if(_value.getData() != null) {
            writeBuffer.pushContext("data", WithReaderWriterArgs.WithRenderAsList(true));
            int itemCount = (int) _value.getData().length;
            int curItem = 0;
            for(byte element : _value.getData()) {
                writeBuffer.writeSignedByte("", 8, ((Number) element).byteValue());
                curItem++;
            }
            writeBuffer.popContext("data", WithReaderWriterArgs.WithRenderAsList(true));
        }
        writeBuffer.popContext("AdsWriteRequest");
    }

    public static class AdsWriteRequestBuilder implements AdsDataIO.AdsDataBuilder {
        private final long indexGroup;
        private final long indexOffset;
        private final byte[] data;

        public AdsWriteRequestBuilder(long indexGroup, long indexOffset, byte[] data) {
            this.indexGroup = indexGroup;
            this.indexOffset = indexOffset;
            this.data = data;
        }

        public AdsWriteRequest build() {
            return new AdsWriteRequest(indexGroup, indexOffset, data);
        }
    }

}
