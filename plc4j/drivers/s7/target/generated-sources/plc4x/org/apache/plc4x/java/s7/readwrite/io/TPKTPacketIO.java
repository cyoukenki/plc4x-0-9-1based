/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.s7.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.s7.readwrite.*;
import org.apache.plc4x.java.s7.readwrite.io.*;
import org.apache.plc4x.java.s7.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class TPKTPacketIO implements MessageIO<TPKTPacket, TPKTPacket> {

    private static final Logger LOGGER = LoggerFactory.getLogger(TPKTPacketIO.class);

    @Override
    public TPKTPacket parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return TPKTPacketIO.staticParse(readBuffer);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, TPKTPacket value, Object... args) throws ParseException {
        TPKTPacketIO.staticSerialize(writeBuffer, value);
    }

    public static TPKTPacket staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("TPKTPacket");
        int startPos = readBuffer.getPos();
        int curPos;

        // Const Field (protocolId)
        short protocolId = readBuffer.readUnsignedShort("protocolId", 8);
        if(protocolId != TPKTPacket.PROTOCOLID) {
            throw new ParseException("Expected constant value " + TPKTPacket.PROTOCOLID + " but got " + protocolId);
        }

        // Reserved Field (Compartmentalized so the "reserved" variable can't leak)
        {
            short reserved = readBuffer.readUnsignedShort("reserved", 8);
            if(reserved != (short) 0x00) {
                LOGGER.info("Expected constant value " + 0x00 + " but got " + reserved + " for reserved field.");
            }
        }

        // Implicit Field (len) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        int len = readBuffer.readUnsignedInt("len", 16);

        readBuffer.pullContext("payload");

        // Simple Field (payload)
COTPPacket payload = COTPPacketIO.staticParse(readBuffer , (int) ((len) - (4)) ) ;        readBuffer.closeContext("payload");

        readBuffer.closeContext("TPKTPacket");
        // Create the instance
        return new TPKTPacket(payload);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, TPKTPacket _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("TPKTPacket");

        // Const Field (protocolId)
        writeBuffer.writeUnsignedShort("protocolId", 8, ((Number) 0x03).shortValue());

        // Reserved Field (reserved)
        writeBuffer.writeUnsignedShort("reserved", 8, ((Number) (short) 0x00).shortValue());

        // Implicit Field (len) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        int len = (int) ((_value.getPayload().getLengthInBytes()) + (4));
        writeBuffer.writeUnsignedInt("len", 16, ((Number) (len)).intValue());

        // Simple Field (payload)
        COTPPacket payload = (COTPPacket) _value.getPayload();
        writeBuffer.pushContext("payload");
        COTPPacketIO.staticSerialize(writeBuffer, payload);
        writeBuffer.popContext("payload");
        writeBuffer.popContext("TPKTPacket");
    }

}
