/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.simulated.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.simulated.readwrite.*;
import org.apache.plc4x.java.simulated.readwrite.io.*;
import org.apache.plc4x.java.simulated.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class VidDataIO implements MessageIO<VidData, VidData> {

    private static final Logger LOGGER = LoggerFactory.getLogger(VidDataIO.class);

    @Override
    public VidData parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return VidDataIO.staticParse(readBuffer);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, VidData value, Object... args) throws ParseException {
        VidDataIO.staticSerialize(writeBuffer, value);
    }

    public static VidData staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("VidData");
        int startPos = readBuffer.getPos();
        int curPos;

        // Const Field (L11)
        short L11 = readBuffer.readUnsignedShort("L11", 8);
        if(L11 != VidData.L11) {
            throw new ParseException("Expected constant value " + VidData.L11 + " but got " + L11);
        }


        // Simple Field (dataLen)
short dataLen = readBuffer.readUnsignedShort("dataLen", 8) ;        // Array field (values)
        readBuffer.pullContext("values", WithReaderWriterArgs.WithRenderAsList(true));
        // Count array
        if(dataLen > Integer.MAX_VALUE) {
            throw new ParseException("Array count of " + (dataLen) + " exceeds the maximum allowed count of " + Integer.MAX_VALUE);
        }
        DataStruct[] values;
        {
            int itemCount = Math.max(0, (int) dataLen);
            values = new DataStruct[itemCount];
            for(int curItem = 0; curItem < itemCount; curItem++) {
                boolean lastItem = curItem == (itemCount - 1);
values[curItem] = DataStructIO.staticParse(readBuffer ) ;            }
        }
            readBuffer.closeContext("values", WithReaderWriterArgs.WithRenderAsList(true));

        readBuffer.closeContext("VidData");
        // Create the instance
        return new VidData(dataLen, values);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, VidData _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("VidData");

        // Const Field (L11)
        writeBuffer.writeUnsignedShort("L11", 8, ((Number) 0x01).shortValue());

        // Simple Field (dataLen)
        short dataLen = (short) _value.getDataLen();
        writeBuffer.writeUnsignedShort("dataLen", 8, ((Number) (dataLen)).shortValue());

        // Array Field (values)
        if(_value.getValues() != null) {
            writeBuffer.pushContext("values", WithReaderWriterArgs.WithRenderAsList(true));
            int itemCount = (int) _value.getValues().length;
            int curItem = 0;
            for(DataStruct element : _value.getValues()) {
                boolean lastItem = curItem == (itemCount - 1);
                DataStructIO.staticSerialize(writeBuffer, element);
                curItem++;
            }
            writeBuffer.popContext("values", WithReaderWriterArgs.WithRenderAsList(true));
        }
        writeBuffer.popContext("VidData");
    }

}
