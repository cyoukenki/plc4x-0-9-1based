/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.modbus.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.modbus.readwrite.*;
import org.apache.plc4x.java.modbus.readwrite.io.*;
import org.apache.plc4x.java.modbus.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class ModbusSerialADUIO implements MessageIO<ModbusSerialADU, ModbusSerialADU> {

    private static final Logger LOGGER = LoggerFactory.getLogger(ModbusSerialADUIO.class);

    @Override
    public ModbusSerialADU parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        if((args == null) || (args.length != 1)) {
            throw new PlcRuntimeException("Wrong number of arguments, expected 1, but got " + args.length);
        }
        Boolean response;
        if(args[0] instanceof Boolean) {
            response = (Boolean) args[0];
        } else if (args[0] instanceof String) {
            response = Boolean.valueOf((String) args[0]);
        } else {
            throw new PlcRuntimeException("Argument 0 expected to be of type Boolean or a string which is parseable but was " + args[0].getClass().getName());
        }
        return ModbusSerialADUIO.staticParse(readBuffer, response);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, ModbusSerialADU value, Object... args) throws ParseException {
        ModbusSerialADUIO.staticSerialize(writeBuffer, value);
    }

    public static ModbusSerialADU staticParse(ReadBuffer readBuffer, Boolean response) throws ParseException {
        readBuffer.pullContext("ModbusSerialADU");
        int startPos = readBuffer.getPos();
        int curPos;


        // Simple Field (transactionId)
int transactionId = readBuffer.readUnsignedInt("transactionId", 16) ;
        // Reserved Field (Compartmentalized so the "reserved" variable can't leak)
        {
            int reserved = readBuffer.readUnsignedInt("reserved", 16);
            if(reserved != (int) 0x0000) {
                LOGGER.info("Expected constant value " + 0x0000 + " but got " + reserved + " for reserved field.");
            }
        }


        // Simple Field (length)
int length = readBuffer.readUnsignedInt("length", 16) ;

        // Simple Field (address)
short address = readBuffer.readUnsignedShort("address", 8) ;
        readBuffer.pullContext("pdu");

        // Simple Field (pdu)
ModbusPDU pdu = ModbusPDUIO.staticParse(readBuffer , (boolean) (response) ) ;        readBuffer.closeContext("pdu");

        readBuffer.closeContext("ModbusSerialADU");
        // Create the instance
        return new ModbusSerialADU(transactionId, length, address, pdu);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, ModbusSerialADU _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("ModbusSerialADU");

        // Simple Field (transactionId)
        int transactionId = (int) _value.getTransactionId();
        writeBuffer.writeUnsignedInt("transactionId", 16, ((Number) (transactionId)).intValue());

        // Reserved Field (reserved)
        writeBuffer.writeUnsignedInt("reserved", 16, ((Number) (int) 0x0000).intValue());

        // Simple Field (length)
        int length = (int) _value.getLength();
        writeBuffer.writeUnsignedInt("length", 16, ((Number) (length)).intValue());

        // Simple Field (address)
        short address = (short) _value.getAddress();
        writeBuffer.writeUnsignedShort("address", 8, ((Number) (address)).shortValue());

        // Simple Field (pdu)
        ModbusPDU pdu = (ModbusPDU) _value.getPdu();
        writeBuffer.pushContext("pdu");
        ModbusPDUIO.staticSerialize(writeBuffer, pdu);
        writeBuffer.popContext("pdu");
        writeBuffer.popContext("ModbusSerialADU");
    }

}
