/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.modbus.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.modbus.readwrite.*;
import org.apache.plc4x.java.modbus.readwrite.io.*;
import org.apache.plc4x.java.modbus.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class ModbusPDUGetComEventLogResponseIO implements MessageIO<ModbusPDUGetComEventLogResponse, ModbusPDUGetComEventLogResponse> {

    private static final Logger LOGGER = LoggerFactory.getLogger(ModbusPDUGetComEventLogResponseIO.class);

    @Override
    public ModbusPDUGetComEventLogResponse parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return (ModbusPDUGetComEventLogResponse) new ModbusPDUIO().parse(readBuffer, args);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, ModbusPDUGetComEventLogResponse value, Object... args) throws ParseException {
        new ModbusPDUIO().serialize(writeBuffer, value, args);
    }

    public static ModbusPDUGetComEventLogResponseBuilder staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("ModbusPDUGetComEventLogResponse");
        int startPos = readBuffer.getPos();
        int curPos;

        // Implicit Field (byteCount) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        short byteCount = readBuffer.readUnsignedShort("byteCount", 8);


        // Simple Field (status)
int status = readBuffer.readUnsignedInt("status", 16) ;

        // Simple Field (eventCount)
int eventCount = readBuffer.readUnsignedInt("eventCount", 16) ;

        // Simple Field (messageCount)
int messageCount = readBuffer.readUnsignedInt("messageCount", 16) ;        // Array field (events)
        readBuffer.pullContext("events", WithReaderWriterArgs.WithRenderAsList(true));
        // Count array
        if((byteCount) - (6) > Integer.MAX_VALUE) {
            throw new ParseException("Array count of " + ((byteCount) - (6)) + " exceeds the maximum allowed count of " + Integer.MAX_VALUE);
        }
        byte[] events;
        {
            int itemCount = Math.max(0, (int) (byteCount) - (6));
            events = new byte[itemCount];
            for(int curItem = 0; curItem < itemCount; curItem++) {
                
events[curItem] = readBuffer.readSignedByte("", 8) ;            }
        }
            readBuffer.closeContext("events", WithReaderWriterArgs.WithRenderAsList(true));

        readBuffer.closeContext("ModbusPDUGetComEventLogResponse");
        // Create the instance
        return new ModbusPDUGetComEventLogResponseBuilder(status, eventCount, messageCount, events);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, ModbusPDUGetComEventLogResponse _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("ModbusPDUGetComEventLogResponse");

        // Implicit Field (byteCount) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        short byteCount = (short) ((COUNT(_value.getEvents())) + (6));
        writeBuffer.writeUnsignedShort("byteCount", 8, ((Number) (byteCount)).shortValue());

        // Simple Field (status)
        int status = (int) _value.getStatus();
        writeBuffer.writeUnsignedInt("status", 16, ((Number) (status)).intValue());

        // Simple Field (eventCount)
        int eventCount = (int) _value.getEventCount();
        writeBuffer.writeUnsignedInt("eventCount", 16, ((Number) (eventCount)).intValue());

        // Simple Field (messageCount)
        int messageCount = (int) _value.getMessageCount();
        writeBuffer.writeUnsignedInt("messageCount", 16, ((Number) (messageCount)).intValue());

        // Array Field (events)
        if(_value.getEvents() != null) {
            writeBuffer.pushContext("events", WithReaderWriterArgs.WithRenderAsList(true));
            int itemCount = (int) _value.getEvents().length;
            int curItem = 0;
            for(byte element : _value.getEvents()) {
                writeBuffer.writeSignedByte("", 8, ((Number) element).byteValue());
                curItem++;
            }
            writeBuffer.popContext("events", WithReaderWriterArgs.WithRenderAsList(true));
        }
        writeBuffer.popContext("ModbusPDUGetComEventLogResponse");
    }

    public static class ModbusPDUGetComEventLogResponseBuilder implements ModbusPDUIO.ModbusPDUBuilder {
        private final int status;
        private final int eventCount;
        private final int messageCount;
        private final byte[] events;

        public ModbusPDUGetComEventLogResponseBuilder(int status, int eventCount, int messageCount, byte[] events) {
            this.status = status;
            this.eventCount = eventCount;
            this.messageCount = messageCount;
            this.events = events;
        }

        public ModbusPDUGetComEventLogResponse build() {
            return new ModbusPDUGetComEventLogResponse(status, eventCount, messageCount, events);
        }
    }

}
