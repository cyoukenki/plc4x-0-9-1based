/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.modbus.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.modbus.readwrite.*;
import org.apache.plc4x.java.modbus.readwrite.io.*;
import org.apache.plc4x.java.modbus.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class ModbusPDUReadFileRecordRequestIO implements MessageIO<ModbusPDUReadFileRecordRequest, ModbusPDUReadFileRecordRequest> {

    private static final Logger LOGGER = LoggerFactory.getLogger(ModbusPDUReadFileRecordRequestIO.class);

    @Override
    public ModbusPDUReadFileRecordRequest parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return (ModbusPDUReadFileRecordRequest) new ModbusPDUIO().parse(readBuffer, args);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, ModbusPDUReadFileRecordRequest value, Object... args) throws ParseException {
        new ModbusPDUIO().serialize(writeBuffer, value, args);
    }

    public static ModbusPDUReadFileRecordRequestBuilder staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("ModbusPDUReadFileRecordRequest");
        int startPos = readBuffer.getPos();
        int curPos;

        // Implicit Field (byteCount) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        short byteCount = readBuffer.readUnsignedShort("byteCount", 8);
        // Array field (items)
        readBuffer.pullContext("items", WithReaderWriterArgs.WithRenderAsList(true));
        // Length array
        long _itemsLength = byteCount;
        List<ModbusPDUReadFileRecordRequestItem> _itemsList = new LinkedList<>();
        long itemsEndPos = readBuffer.getPos() + _itemsLength;
        while(readBuffer.getPos() < itemsEndPos) {
            _itemsList.add(ModbusPDUReadFileRecordRequestItemIO.staticParse(readBuffer));
        }
        ModbusPDUReadFileRecordRequestItem[] items = _itemsList.toArray(new ModbusPDUReadFileRecordRequestItem[0]);
            readBuffer.closeContext("items", WithReaderWriterArgs.WithRenderAsList(true));

        readBuffer.closeContext("ModbusPDUReadFileRecordRequest");
        // Create the instance
        return new ModbusPDUReadFileRecordRequestBuilder(items);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, ModbusPDUReadFileRecordRequest _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("ModbusPDUReadFileRecordRequest");

        // Implicit Field (byteCount) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        short byteCount = (short) (ARRAY_SIZE_IN_BYTES(_value.getItems()));
        writeBuffer.writeUnsignedShort("byteCount", 8, ((Number) (byteCount)).shortValue());

        // Array Field (items)
        if(_value.getItems() != null) {
            writeBuffer.pushContext("items", WithReaderWriterArgs.WithRenderAsList(true));
            int itemCount = (int) _value.getItems().length;
            int curItem = 0;
            for(ModbusPDUReadFileRecordRequestItem element : _value.getItems()) {
                boolean lastItem = curItem == (itemCount - 1);
                ModbusPDUReadFileRecordRequestItemIO.staticSerialize(writeBuffer, element);
                curItem++;
            }
            writeBuffer.popContext("items", WithReaderWriterArgs.WithRenderAsList(true));
        }
        writeBuffer.popContext("ModbusPDUReadFileRecordRequest");
    }

    public static class ModbusPDUReadFileRecordRequestBuilder implements ModbusPDUIO.ModbusPDUBuilder {
        private final ModbusPDUReadFileRecordRequestItem[] items;

        public ModbusPDUReadFileRecordRequestBuilder(ModbusPDUReadFileRecordRequestItem[] items) {
            this.items = items;
        }

        public ModbusPDUReadFileRecordRequest build() {
            return new ModbusPDUReadFileRecordRequest(items);
        }
    }

}
