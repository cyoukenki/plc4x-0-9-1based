/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.opcua.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.opcua.readwrite.*;
import org.apache.plc4x.java.opcua.readwrite.io.*;
import org.apache.plc4x.java.opcua.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class EventNotificationListIO implements MessageIO<EventNotificationList, EventNotificationList> {

    private static final Logger LOGGER = LoggerFactory.getLogger(EventNotificationListIO.class);

    @Override
    public EventNotificationList parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return (EventNotificationList) new ExtensionObjectDefinitionIO().parse(readBuffer, args);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, EventNotificationList value, Object... args) throws ParseException {
        new ExtensionObjectDefinitionIO().serialize(writeBuffer, value, args);
    }

    public static EventNotificationListBuilder staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("EventNotificationList");
        int startPos = readBuffer.getPos();
        int curPos;

        // Implicit Field (notificationLength) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        int notificationLength = readBuffer.readInt("notificationLength", 32);


        // Simple Field (noOfEvents)
int noOfEvents = readBuffer.readInt("noOfEvents", 32) ;        // Array field (events)
        readBuffer.pullContext("events", WithReaderWriterArgs.WithRenderAsList(true));
        // Count array
        if(noOfEvents > Integer.MAX_VALUE) {
            throw new ParseException("Array count of " + (noOfEvents) + " exceeds the maximum allowed count of " + Integer.MAX_VALUE);
        }
        ExtensionObjectDefinition[] events;
        {
            int itemCount = Math.max(0, (int) noOfEvents);
            events = new ExtensionObjectDefinition[itemCount];
            for(int curItem = 0; curItem < itemCount; curItem++) {
                boolean lastItem = curItem == (itemCount - 1);
events[curItem] = ExtensionObjectDefinitionIO.staticParse(readBuffer , String.valueOf(919) ) ;            }
        }
            readBuffer.closeContext("events", WithReaderWriterArgs.WithRenderAsList(true));

        readBuffer.closeContext("EventNotificationList");
        // Create the instance
        return new EventNotificationListBuilder(noOfEvents, events);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, EventNotificationList _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("EventNotificationList");

        // Implicit Field (notificationLength) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        int notificationLength = (int) (_value.getLengthInBytes());
        writeBuffer.writeInt("notificationLength", 32, ((Number) (notificationLength)).intValue());

        // Simple Field (noOfEvents)
        int noOfEvents = (int) _value.getNoOfEvents();
        writeBuffer.writeInt("noOfEvents", 32, ((Number) (noOfEvents)).intValue());

        // Array Field (events)
        if(_value.getEvents() != null) {
            writeBuffer.pushContext("events", WithReaderWriterArgs.WithRenderAsList(true));
            int itemCount = (int) _value.getEvents().length;
            int curItem = 0;
            for(ExtensionObjectDefinition element : _value.getEvents()) {
                boolean lastItem = curItem == (itemCount - 1);
                ExtensionObjectDefinitionIO.staticSerialize(writeBuffer, element);
                curItem++;
            }
            writeBuffer.popContext("events", WithReaderWriterArgs.WithRenderAsList(true));
        }
        writeBuffer.popContext("EventNotificationList");
    }

    public static class EventNotificationListBuilder implements ExtensionObjectDefinitionIO.ExtensionObjectDefinitionBuilder {
        private final int noOfEvents;
        private final ExtensionObjectDefinition[] events;

        public EventNotificationListBuilder(int noOfEvents, ExtensionObjectDefinition[] events) {
            this.noOfEvents = noOfEvents;
            this.events = events;
        }

        public EventNotificationList build() {
            return new EventNotificationList(noOfEvents, events);
        }
    }

}
