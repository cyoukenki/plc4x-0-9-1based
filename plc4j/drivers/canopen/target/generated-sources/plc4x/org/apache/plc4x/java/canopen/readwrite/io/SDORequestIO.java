/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.canopen.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.canopen.readwrite.*;
import org.apache.plc4x.java.canopen.readwrite.io.*;
import org.apache.plc4x.java.canopen.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class SDORequestIO implements MessageIO<SDORequest, SDORequest> {

    private static final Logger LOGGER = LoggerFactory.getLogger(SDORequestIO.class);

    @Override
    public SDORequest parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        if((args == null) || (args.length != 1)) {
            throw new PlcRuntimeException("Wrong number of arguments, expected 1, but got " + args.length);
        }
        SDORequestCommand command;
        if(args[0] instanceof SDORequestCommand) {
            command = (SDORequestCommand) args[0];
        } else {
            throw new PlcRuntimeException("Argument 0 expected to be of type SDORequestCommand or a string which is parseable but was " + args[0].getClass().getName());
        }
        return SDORequestIO.staticParse(readBuffer, command);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, SDORequest value, Object... args) throws ParseException {
        SDORequestIO.staticSerialize(writeBuffer, value);
    }

    public static SDORequest staticParse(ReadBuffer readBuffer, SDORequestCommand command) throws ParseException {
        readBuffer.pullContext("SDORequest");
        int startPos = readBuffer.getPos();
        int curPos;

        // Switch Field (Depending on the discriminator values, passes the instantiation to a sub-type)
        SDORequestBuilder builder = null;
                if(EvaluationHelper.equals(command, SDORequestCommand.SEGMENT_DOWNLOAD)) {
            builder = SDOSegmentDownloadRequestIO.staticParse(readBuffer);
        } else 
                if(EvaluationHelper.equals(command, SDORequestCommand.INITIATE_DOWNLOAD)) {
            builder = SDOInitiateDownloadRequestIO.staticParse(readBuffer);
        } else 
                if(EvaluationHelper.equals(command, SDORequestCommand.INITIATE_UPLOAD)) {
            builder = SDOInitiateUploadRequestIO.staticParse(readBuffer);
        } else 
                if(EvaluationHelper.equals(command, SDORequestCommand.SEGMENT_UPLOAD)) {
            builder = SDOSegmentUploadRequestIO.staticParse(readBuffer);
        } else 
                if(EvaluationHelper.equals(command, SDORequestCommand.ABORT)) {
            builder = SDOAbortRequestIO.staticParse(readBuffer);
        } else 
                if(EvaluationHelper.equals(command, SDORequestCommand.BLOCK)) {
            builder = SDOBlockRequestIO.staticParse(readBuffer);
        }
        if (builder == null) {
            throw new ParseException("Unsupported case for discriminated type");
        }

        readBuffer.closeContext("SDORequest");
        // Create the instance
        return builder.build();
    }

    public static void staticSerialize(WriteBuffer writeBuffer, SDORequest _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("SDORequest");

        // Switch field (Depending on the discriminator values, passes the instantiation to a sub-type)
        if(_value instanceof SDOSegmentDownloadRequest) {
            SDOSegmentDownloadRequestIO.staticSerialize(writeBuffer, (SDOSegmentDownloadRequest) _value);
        } else 
        if(_value instanceof SDOInitiateDownloadRequest) {
            SDOInitiateDownloadRequestIO.staticSerialize(writeBuffer, (SDOInitiateDownloadRequest) _value);
        } else 
        if(_value instanceof SDOInitiateUploadRequest) {
            SDOInitiateUploadRequestIO.staticSerialize(writeBuffer, (SDOInitiateUploadRequest) _value);
        } else 
        if(_value instanceof SDOSegmentUploadRequest) {
            SDOSegmentUploadRequestIO.staticSerialize(writeBuffer, (SDOSegmentUploadRequest) _value);
        } else 
        if(_value instanceof SDOAbortRequest) {
            SDOAbortRequestIO.staticSerialize(writeBuffer, (SDOAbortRequest) _value);
        } else 
        if(_value instanceof SDOBlockRequest) {
            SDOBlockRequestIO.staticSerialize(writeBuffer, (SDOBlockRequest) _value);
        }
        writeBuffer.popContext("SDORequest");
    }

    public static interface SDORequestBuilder {
        SDORequest build();
    }

}
