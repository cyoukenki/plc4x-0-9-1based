/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.mc.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.mc.readwrite.*;
import org.apache.plc4x.java.mc.readwrite.io.*;
import org.apache.plc4x.java.mc.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class EndcodeTypeIO implements MessageIO<EndcodeType, EndcodeType> {

    private static final Logger LOGGER = LoggerFactory.getLogger(EndcodeTypeIO.class);

    @Override
    public EndcodeType parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        if((args == null) || (args.length != 1)) {
            throw new PlcRuntimeException("Wrong number of arguments, expected 1, but got " + args.length);
        }
        Integer responsedatalength;
        if(args[0] instanceof Integer) {
            responsedatalength = (Integer) args[0];
        } else if (args[0] instanceof String) {
            responsedatalength = Integer.valueOf((String) args[0]);
        } else {
            throw new PlcRuntimeException("Argument 0 expected to be of type Integer or a string which is parseable but was " + args[0].getClass().getName());
        }
        return EndcodeTypeIO.staticParse(readBuffer, responsedatalength);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, EndcodeType value, Object... args) throws ParseException {
        EndcodeTypeIO.staticSerialize(writeBuffer, value);
    }

    public static EndcodeType staticParse(ReadBuffer readBuffer, Integer responsedatalength) throws ParseException {
        readBuffer.pullContext("EndcodeType");
        int startPos = readBuffer.getPos();
        int curPos;

        // Discriminator Field (Endcode) (Used as input to a switch field)
        long Endcode = readBuffer.readUnsignedLong("Endcode", 32);


        // Switch Field (Depending on the discriminator values, passes the instantiation to a sub-type)
        EndcodeTypeBuilder builder = null;
                if(EvaluationHelper.equals(Endcode, 0x30303030)) {
            builder = NormalendcodeIO.staticParse(readBuffer, responsedatalength);
        } else 
                {
            builder = DefaultUnKnownErrorIO.staticParse(readBuffer);
        }
        if (builder == null) {
            throw new ParseException("Unsupported case for discriminated type");
        }

        readBuffer.closeContext("EndcodeType");
        // Create the instance
        return builder.build();
    }

    public static void staticSerialize(WriteBuffer writeBuffer, EndcodeType _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("EndcodeType");

        // Discriminator Field (Endcode) (Used as input to a switch field)
        long Endcode = (long) _value.getEndcode();
            writeBuffer.writeUnsignedLong("Endcode", 32, ((Number) (Endcode)).longValue());

        // Switch field (Depending on the discriminator values, passes the instantiation to a sub-type)
        if(_value instanceof Normalendcode) {
            NormalendcodeIO.staticSerialize(writeBuffer, (Normalendcode) _value);
        } else 
        if(_value instanceof DefaultUnKnownError) {
            DefaultUnKnownErrorIO.staticSerialize(writeBuffer, (DefaultUnKnownError) _value);
        }
        writeBuffer.popContext("EndcodeType");
    }

    public static interface EndcodeTypeBuilder {
        EndcodeType build();
    }

}
