/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.mc.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.mc.readwrite.*;
import org.apache.plc4x.java.mc.readwrite.io.*;
import org.apache.plc4x.java.mc.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class McRequestIO implements MessageIO<McRequest, McRequest> {

    private static final Logger LOGGER = LoggerFactory.getLogger(McRequestIO.class);

    @Override
    public McRequest parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return (McRequest) new McPacketIO().parse(readBuffer, args);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, McRequest value, Object... args) throws ParseException {
        new McPacketIO().serialize(writeBuffer, value, args);
    }

    public static McRequestBuilder staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("McRequest");
        int startPos = readBuffer.getPos();
        int curPos;

        // Implicit Field (requestdatalength) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        long requestdatalength = readBuffer.readUnsignedLong("requestdatalength", 32);

        // Const Field (monitoringtimer)
        long monitoringtimer = readBuffer.readUnsignedLong("monitoringtimer", 32);
        if(monitoringtimer != McRequest.MONITORINGTIMER) {
            throw new ParseException("Expected constant value " + McRequest.MONITORINGTIMER + " but got " + monitoringtimer);
        }

        readBuffer.pullContext("command");

        // Simple Field (command)
CommandType command = CommandTypeIO.staticParse(readBuffer ) ;        readBuffer.closeContext("command");

        readBuffer.closeContext("McRequest");
        // Create the instance
        return new McRequestBuilder(command);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, McRequest _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("McRequest");

        // Implicit Field (requestdatalength) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        long requestdatalength = (long) (org.apache.plc4x.java.mc.readwrite.util.ConvertUtils.calculateRequestDateLength(_value.getLengthInBytes()));
        writeBuffer.writeUnsignedLong("requestdatalength", 32, ((Number) (requestdatalength)).longValue());

        // Const Field (monitoringtimer)
        writeBuffer.writeUnsignedLong("monitoringtimer", 32, ((Number) 0x30303130).longValue());

        // Simple Field (command)
        CommandType command = (CommandType) _value.getCommand();
        writeBuffer.pushContext("command");
        CommandTypeIO.staticSerialize(writeBuffer, command);
        writeBuffer.popContext("command");
        writeBuffer.popContext("McRequest");
    }

    public static class McRequestBuilder implements McPacketIO.McPacketBuilder {
        private final CommandType command;

        public McRequestBuilder(CommandType command) {
            this.command = command;
        }

        public McRequest build() {
            return new McRequest(command);
        }
    }

}
