/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.secsgem.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.secsgem.readwrite.*;
import org.apache.plc4x.java.secsgem.readwrite.io.*;
import org.apache.plc4x.java.secsgem.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class CEIDDataIO implements MessageIO<CEIDData, CEIDData> {

    private static final Logger LOGGER = LoggerFactory.getLogger(CEIDDataIO.class);

    @Override
    public CEIDData parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return CEIDDataIO.staticParse(readBuffer);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, CEIDData value, Object... args) throws ParseException {
        CEIDDataIO.staticSerialize(writeBuffer, value);
    }

    public static CEIDData staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("CEIDData");
        int startPos = readBuffer.getPos();
        int curPos;

        // Const Field (L2)
        int L2 = readBuffer.readUnsignedInt("L2", 16);
        if(L2 != CEIDData.L2) {
            throw new ParseException("Expected constant value " + CEIDData.L2 + " but got " + L2);
        }

        readBuffer.pullContext("CEID");

        // Simple Field (CEID)
DataStruct CEID = DataStructIO.staticParse(readBuffer ) ;        readBuffer.closeContext("CEID");

        // Const Field (L11)
        short L11 = readBuffer.readUnsignedShort("L11", 8);
        if(L11 != CEIDData.L11) {
            throw new ParseException("Expected constant value " + CEIDData.L11 + " but got " + L11);
        }


        // Simple Field (rptLen)
short rptLen = readBuffer.readUnsignedShort("rptLen", 8) ;        // Array field (rptID)
        readBuffer.pullContext("rptID", WithReaderWriterArgs.WithRenderAsList(true));
        // Count array
        if(rptLen > Integer.MAX_VALUE) {
            throw new ParseException("Array count of " + (rptLen) + " exceeds the maximum allowed count of " + Integer.MAX_VALUE);
        }
        DataStruct[] rptID;
        {
            int itemCount = Math.max(0, (int) rptLen);
            rptID = new DataStruct[itemCount];
            for(int curItem = 0; curItem < itemCount; curItem++) {
                boolean lastItem = curItem == (itemCount - 1);
rptID[curItem] = DataStructIO.staticParse(readBuffer ) ;            }
        }
            readBuffer.closeContext("rptID", WithReaderWriterArgs.WithRenderAsList(true));

        readBuffer.closeContext("CEIDData");
        // Create the instance
        return new CEIDData(CEID, rptLen, rptID);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, CEIDData _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("CEIDData");

        // Const Field (L2)
        writeBuffer.writeUnsignedInt("L2", 16, ((Number) 0x0102).intValue());

        // Simple Field (CEID)
        DataStruct CEID = (DataStruct) _value.getCEID();
        writeBuffer.pushContext("CEID");
        DataStructIO.staticSerialize(writeBuffer, CEID);
        writeBuffer.popContext("CEID");

        // Const Field (L11)
        writeBuffer.writeUnsignedShort("L11", 8, ((Number) 0x01).shortValue());

        // Simple Field (rptLen)
        short rptLen = (short) _value.getRptLen();
        writeBuffer.writeUnsignedShort("rptLen", 8, ((Number) (rptLen)).shortValue());

        // Array Field (rptID)
        if(_value.getRptID() != null) {
            writeBuffer.pushContext("rptID", WithReaderWriterArgs.WithRenderAsList(true));
            int itemCount = (int) _value.getRptID().length;
            int curItem = 0;
            for(DataStruct element : _value.getRptID()) {
                boolean lastItem = curItem == (itemCount - 1);
                DataStructIO.staticSerialize(writeBuffer, element);
                curItem++;
            }
            writeBuffer.popContext("rptID", WithReaderWriterArgs.WithRenderAsList(true));
        }
        writeBuffer.popContext("CEIDData");
    }

}
