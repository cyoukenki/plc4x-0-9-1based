/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.profinet.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.profinet.readwrite.*;
import org.apache.plc4x.java.profinet.readwrite.io.*;
import org.apache.plc4x.java.profinet.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class PnIoCm_ModuleDiffBlockApiIO implements MessageIO<PnIoCm_ModuleDiffBlockApi, PnIoCm_ModuleDiffBlockApi> {

    private static final Logger LOGGER = LoggerFactory.getLogger(PnIoCm_ModuleDiffBlockApiIO.class);

    @Override
    public PnIoCm_ModuleDiffBlockApi parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return PnIoCm_ModuleDiffBlockApiIO.staticParse(readBuffer);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, PnIoCm_ModuleDiffBlockApi value, Object... args) throws ParseException {
        PnIoCm_ModuleDiffBlockApiIO.staticSerialize(writeBuffer, value);
    }

    public static PnIoCm_ModuleDiffBlockApi staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("PnIoCm_ModuleDiffBlockApi");
        int startPos = readBuffer.getPos();
        int curPos;

        // Const Field (api)
        long api = readBuffer.readUnsignedLong("api", 32);
        if(api != PnIoCm_ModuleDiffBlockApi.API) {
            throw new ParseException("Expected constant value " + PnIoCm_ModuleDiffBlockApi.API + " but got " + api);
        }

        // Implicit Field (numModules) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        int numModules = readBuffer.readUnsignedInt("numModules", 16);
        // Array field (modules)
        readBuffer.pullContext("modules", WithReaderWriterArgs.WithRenderAsList(true));
        // Count array
        if(numModules > Integer.MAX_VALUE) {
            throw new ParseException("Array count of " + (numModules) + " exceeds the maximum allowed count of " + Integer.MAX_VALUE);
        }
        PnIoCm_ModuleDiffBlockApi_Module[] modules;
        {
            int itemCount = Math.max(0, (int) numModules);
            modules = new PnIoCm_ModuleDiffBlockApi_Module[itemCount];
            for(int curItem = 0; curItem < itemCount; curItem++) {
                boolean lastItem = curItem == (itemCount - 1);
modules[curItem] = PnIoCm_ModuleDiffBlockApi_ModuleIO.staticParse(readBuffer ) ;            }
        }
            readBuffer.closeContext("modules", WithReaderWriterArgs.WithRenderAsList(true));

        readBuffer.closeContext("PnIoCm_ModuleDiffBlockApi");
        // Create the instance
        return new PnIoCm_ModuleDiffBlockApi(modules);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, PnIoCm_ModuleDiffBlockApi _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("PnIoCm_ModuleDiffBlockApi");

        // Const Field (api)
        writeBuffer.writeUnsignedLong("api", 32, ((Number) 0x00000000).longValue());

        // Implicit Field (numModules) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        int numModules = (int) (COUNT(_value.getModules()));
        writeBuffer.writeUnsignedInt("numModules", 16, ((Number) (numModules)).intValue());

        // Array Field (modules)
        if(_value.getModules() != null) {
            writeBuffer.pushContext("modules", WithReaderWriterArgs.WithRenderAsList(true));
            int itemCount = (int) _value.getModules().length;
            int curItem = 0;
            for(PnIoCm_ModuleDiffBlockApi_Module element : _value.getModules()) {
                boolean lastItem = curItem == (itemCount - 1);
                PnIoCm_ModuleDiffBlockApi_ModuleIO.staticSerialize(writeBuffer, element);
                curItem++;
            }
            writeBuffer.popContext("modules", WithReaderWriterArgs.WithRenderAsList(true));
        }
        writeBuffer.popContext("PnIoCm_ModuleDiffBlockApi");
    }

}
