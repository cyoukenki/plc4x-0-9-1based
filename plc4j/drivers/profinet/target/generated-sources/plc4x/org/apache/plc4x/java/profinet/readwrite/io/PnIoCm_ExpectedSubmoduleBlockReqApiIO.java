/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.profinet.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.profinet.readwrite.*;
import org.apache.plc4x.java.profinet.readwrite.io.*;
import org.apache.plc4x.java.profinet.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class PnIoCm_ExpectedSubmoduleBlockReqApiIO implements MessageIO<PnIoCm_ExpectedSubmoduleBlockReqApi, PnIoCm_ExpectedSubmoduleBlockReqApi> {

    private static final Logger LOGGER = LoggerFactory.getLogger(PnIoCm_ExpectedSubmoduleBlockReqApiIO.class);

    @Override
    public PnIoCm_ExpectedSubmoduleBlockReqApi parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return PnIoCm_ExpectedSubmoduleBlockReqApiIO.staticParse(readBuffer);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, PnIoCm_ExpectedSubmoduleBlockReqApi value, Object... args) throws ParseException {
        PnIoCm_ExpectedSubmoduleBlockReqApiIO.staticSerialize(writeBuffer, value);
    }

    public static PnIoCm_ExpectedSubmoduleBlockReqApi staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("PnIoCm_ExpectedSubmoduleBlockReqApi");
        int startPos = readBuffer.getPos();
        int curPos;

        // Const Field (api)
        long api = readBuffer.readUnsignedLong("api", 32);
        if(api != PnIoCm_ExpectedSubmoduleBlockReqApi.API) {
            throw new ParseException("Expected constant value " + PnIoCm_ExpectedSubmoduleBlockReqApi.API + " but got " + api);
        }


        // Simple Field (slotNumber)
int slotNumber = readBuffer.readUnsignedInt("slotNumber", 16) ;

        // Simple Field (moduleIdentNumber)
long moduleIdentNumber = readBuffer.readUnsignedLong("moduleIdentNumber", 32) ;

        // Simple Field (moduleProperties)
int moduleProperties = readBuffer.readUnsignedInt("moduleProperties", 16) ;
        // Implicit Field (numSubmodules) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        int numSubmodules = readBuffer.readUnsignedInt("numSubmodules", 16);
        // Array field (submodules)
        readBuffer.pullContext("submodules", WithReaderWriterArgs.WithRenderAsList(true));
        // Count array
        if(numSubmodules > Integer.MAX_VALUE) {
            throw new ParseException("Array count of " + (numSubmodules) + " exceeds the maximum allowed count of " + Integer.MAX_VALUE);
        }
        PnIoCm_Submodule[] submodules;
        {
            int itemCount = Math.max(0, (int) numSubmodules);
            submodules = new PnIoCm_Submodule[itemCount];
            for(int curItem = 0; curItem < itemCount; curItem++) {
                boolean lastItem = curItem == (itemCount - 1);
submodules[curItem] = PnIoCm_SubmoduleIO.staticParse(readBuffer ) ;            }
        }
            readBuffer.closeContext("submodules", WithReaderWriterArgs.WithRenderAsList(true));

        readBuffer.closeContext("PnIoCm_ExpectedSubmoduleBlockReqApi");
        // Create the instance
        return new PnIoCm_ExpectedSubmoduleBlockReqApi(slotNumber, moduleIdentNumber, moduleProperties, submodules);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, PnIoCm_ExpectedSubmoduleBlockReqApi _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("PnIoCm_ExpectedSubmoduleBlockReqApi");

        // Const Field (api)
        writeBuffer.writeUnsignedLong("api", 32, ((Number) 0x00000000).longValue());

        // Simple Field (slotNumber)
        int slotNumber = (int) _value.getSlotNumber();
        writeBuffer.writeUnsignedInt("slotNumber", 16, ((Number) (slotNumber)).intValue());

        // Simple Field (moduleIdentNumber)
        long moduleIdentNumber = (long) _value.getModuleIdentNumber();
        writeBuffer.writeUnsignedLong("moduleIdentNumber", 32, ((Number) (moduleIdentNumber)).longValue());

        // Simple Field (moduleProperties)
        int moduleProperties = (int) _value.getModuleProperties();
        writeBuffer.writeUnsignedInt("moduleProperties", 16, ((Number) (moduleProperties)).intValue());

        // Implicit Field (numSubmodules) (Used for parsing, but it's value is not stored as it's implicitly given by the objects content)
        int numSubmodules = (int) (COUNT(_value.getSubmodules()));
        writeBuffer.writeUnsignedInt("numSubmodules", 16, ((Number) (numSubmodules)).intValue());

        // Array Field (submodules)
        if(_value.getSubmodules() != null) {
            writeBuffer.pushContext("submodules", WithReaderWriterArgs.WithRenderAsList(true));
            int itemCount = (int) _value.getSubmodules().length;
            int curItem = 0;
            for(PnIoCm_Submodule element : _value.getSubmodules()) {
                boolean lastItem = curItem == (itemCount - 1);
                PnIoCm_SubmoduleIO.staticSerialize(writeBuffer, element);
                curItem++;
            }
            writeBuffer.popContext("submodules", WithReaderWriterArgs.WithRenderAsList(true));
        }
        writeBuffer.popContext("PnIoCm_ExpectedSubmoduleBlockReqApi");
    }

}
