/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.profinet.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.profinet.readwrite.*;

import org.apache.plc4x.java.profinet.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class PnIoCm_IoCsIO implements MessageIO<PnIoCm_IoCs, PnIoCm_IoCs> {

    private static final Logger LOGGER = LoggerFactory.getLogger(PnIoCm_IoCsIO.class);

    @Override
    public PnIoCm_IoCs parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return PnIoCm_IoCsIO.staticParse(readBuffer);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, PnIoCm_IoCs value, Object... args) throws ParseException {
        PnIoCm_IoCsIO.staticSerialize(writeBuffer, value);
    }

    public static PnIoCm_IoCs staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("PnIoCm_IoCs");
        int startPos = readBuffer.getPos();
        int curPos;


        // Simple Field (slotNumber)
int slotNumber = readBuffer.readUnsignedInt("slotNumber", 16) ;

        // Simple Field (subSlotNumber)
int subSlotNumber = readBuffer.readUnsignedInt("subSlotNumber", 16) ;

        // Simple Field (ioFrameOffset)
int ioFrameOffset = readBuffer.readUnsignedInt("ioFrameOffset", 16) ;
        readBuffer.closeContext("PnIoCm_IoCs");
        // Create the instance
        return new PnIoCm_IoCs(slotNumber, subSlotNumber, ioFrameOffset);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, PnIoCm_IoCs _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("PnIoCm_IoCs");

        // Simple Field (slotNumber)
        int slotNumber = (int) _value.getSlotNumber();
        writeBuffer.writeUnsignedInt("slotNumber", 16, ((Number) (slotNumber)).intValue());

        // Simple Field (subSlotNumber)
        int subSlotNumber = (int) _value.getSubSlotNumber();
        writeBuffer.writeUnsignedInt("subSlotNumber", 16, ((Number) (subSlotNumber)).intValue());

        // Simple Field (ioFrameOffset)
        int ioFrameOffset = (int) _value.getIoFrameOffset();
        writeBuffer.writeUnsignedInt("ioFrameOffset", 16, ((Number) (ioFrameOffset)).intValue());
        writeBuffer.popContext("PnIoCm_IoCs");
    }

}
