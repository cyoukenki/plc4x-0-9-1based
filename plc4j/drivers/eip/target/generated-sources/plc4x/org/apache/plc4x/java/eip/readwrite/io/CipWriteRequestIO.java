/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.eip.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.eip.readwrite.*;
import org.apache.plc4x.java.eip.readwrite.io.*;
import org.apache.plc4x.java.eip.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class CipWriteRequestIO implements MessageIO<CipWriteRequest, CipWriteRequest> {

    private static final Logger LOGGER = LoggerFactory.getLogger(CipWriteRequestIO.class);

    @Override
    public CipWriteRequest parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return (CipWriteRequest) new CipServiceIO().parse(readBuffer, args);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, CipWriteRequest value, Object... args) throws ParseException {
        new CipServiceIO().serialize(writeBuffer, value, args);
    }

    public static CipWriteRequestBuilder staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("CipWriteRequest");
        int startPos = readBuffer.getPos();
        int curPos;


        // Simple Field (RequestPathSize)
byte RequestPathSize = readBuffer.readSignedByte("RequestPathSize", 8) ;        // Array field (tag)
        readBuffer.pullContext("tag", WithReaderWriterArgs.WithRenderAsList(true));
        // Length array
        long _tagLength = ((RequestPathSize) * (2));
        List<Byte> _tagList = new LinkedList<>();
        long tagEndPos = readBuffer.getPos() + _tagLength;
        while(readBuffer.getPos() < tagEndPos) {
            _tagList.add(readBuffer.readSignedByte("", 8));
        }
        byte[] tag = new byte[_tagList.size()];
        for(int i = 0; i < _tagList.size(); i++) {
            tag[i] = (byte) _tagList.get(i);
        }
            readBuffer.closeContext("tag", WithReaderWriterArgs.WithRenderAsList(true));

        readBuffer.pullContext("dataType");
        // Enum field (dataType)
        CIPDataTypeCode dataType = CIPDataTypeCode.enumForValue(readBuffer.readUnsignedInt("CIPDataTypeCode", 16));
        readBuffer.closeContext("dataType");


        // Simple Field (elementNb)
int elementNb = readBuffer.readUnsignedInt("elementNb", 16) ;        // Array field (data)
        readBuffer.pullContext("data", WithReaderWriterArgs.WithRenderAsList(true));
        // Length array
        long _dataLength = (dataType.getSize()) * (elementNb);
        List<Byte> _dataList = new LinkedList<>();
        long dataEndPos = readBuffer.getPos() + _dataLength;
        while(readBuffer.getPos() < dataEndPos) {
            _dataList.add(readBuffer.readSignedByte("", 8));
        }
        byte[] data = new byte[_dataList.size()];
        for(int i = 0; i < _dataList.size(); i++) {
            data[i] = (byte) _dataList.get(i);
        }
            readBuffer.closeContext("data", WithReaderWriterArgs.WithRenderAsList(true));

        readBuffer.closeContext("CipWriteRequest");
        // Create the instance
        return new CipWriteRequestBuilder(RequestPathSize, tag, dataType, elementNb, data);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, CipWriteRequest _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("CipWriteRequest");

        // Simple Field (RequestPathSize)
        byte RequestPathSize = (byte) _value.getRequestPathSize();
        writeBuffer.writeSignedByte("RequestPathSize", 8, ((Number) (RequestPathSize)).byteValue());

        // Array Field (tag)
        if(_value.getTag() != null) {
            writeBuffer.pushContext("tag", WithReaderWriterArgs.WithRenderAsList(true));
            int itemCount = (int) _value.getTag().length;
            int curItem = 0;
            for(byte element : _value.getTag()) {
                writeBuffer.writeSignedByte("", 8, ((Number) element).byteValue());
                curItem++;
            }
            writeBuffer.popContext("tag", WithReaderWriterArgs.WithRenderAsList(true));
        }

        writeBuffer.pushContext("dataType");
        // Enum field (dataType)
        CIPDataTypeCode dataType = (CIPDataTypeCode) _value.getDataType();
        writeBuffer.writeUnsignedInt("CIPDataTypeCode", 16, ((Number) (dataType.getValue())).intValue(), WithReaderWriterArgs.WithAdditionalStringRepresentation(dataType.name()));
        writeBuffer.popContext("dataType");

        // Simple Field (elementNb)
        int elementNb = (int) _value.getElementNb();
        writeBuffer.writeUnsignedInt("elementNb", 16, ((Number) (elementNb)).intValue());

        // Array Field (data)
        if(_value.getData() != null) {
            writeBuffer.pushContext("data", WithReaderWriterArgs.WithRenderAsList(true));
            int itemCount = (int) _value.getData().length;
            int curItem = 0;
            for(byte element : _value.getData()) {
                writeBuffer.writeSignedByte("", 8, ((Number) element).byteValue());
                curItem++;
            }
            writeBuffer.popContext("data", WithReaderWriterArgs.WithRenderAsList(true));
        }
        writeBuffer.popContext("CipWriteRequest");
    }

    public static class CipWriteRequestBuilder implements CipServiceIO.CipServiceBuilder {
        private final byte RequestPathSize;
        private final byte[] tag;
        private final CIPDataTypeCode dataType;
        private final int elementNb;
        private final byte[] data;

        public CipWriteRequestBuilder(byte RequestPathSize, byte[] tag, CIPDataTypeCode dataType, int elementNb, byte[] data) {
            this.RequestPathSize = RequestPathSize;
            this.tag = tag;
            this.dataType = dataType;
            this.elementNb = elementNb;
            this.data = data;
        }

        public CipWriteRequest build() {
            return new CipWriteRequest(RequestPathSize, tag, dataType, elementNb, data);
        }
    }

}
