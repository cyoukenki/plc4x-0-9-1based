/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.eip.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.eip.readwrite.*;
import org.apache.plc4x.java.eip.readwrite.io.*;
import org.apache.plc4x.java.eip.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class LargeForwardOpenRequestIO implements MessageIO<LargeForwardOpenRequest, LargeForwardOpenRequest> {

    private static final Logger LOGGER = LoggerFactory.getLogger(LargeForwardOpenRequestIO.class);

    @Override
    public LargeForwardOpenRequest parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        return (LargeForwardOpenRequest) new CipServiceIO().parse(readBuffer, args);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, LargeForwardOpenRequest value, Object... args) throws ParseException {
        new CipServiceIO().serialize(writeBuffer, value, args);
    }

    public static LargeForwardOpenRequestBuilder staticParse(ReadBuffer readBuffer) throws ParseException {
        readBuffer.pullContext("LargeForwardOpenRequest");
        int startPos = readBuffer.getPos();
        int curPos;

        // Const Field (request_path_size)
        short request_path_size = readBuffer.readUnsignedShort("request_path_size", 8);
        if(request_path_size != LargeForwardOpenRequest.REQUEST_PATH_SIZE) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.REQUEST_PATH_SIZE + " but got " + request_path_size);
        }

        // Const Field (request_path)
        long request_path = readBuffer.readUnsignedLong("request_path", 32);
        if(request_path != LargeForwardOpenRequest.REQUEST_PATH) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.REQUEST_PATH + " but got " + request_path);
        }

        // Const Field (ptt)
        short ptt = readBuffer.readUnsignedShort("ptt", 8);
        if(ptt != LargeForwardOpenRequest.PTT) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.PTT + " but got " + ptt);
        }

        // Const Field (tot)
        short tot = readBuffer.readUnsignedShort("tot", 8);
        if(tot != LargeForwardOpenRequest.TOT) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.TOT + " but got " + tot);
        }


        // Simple Field (o_t_id)
long o_t_id = readBuffer.readUnsignedLong("o_t_id", 32) ;

        // Simple Field (t_o_id)
long t_o_id = readBuffer.readUnsignedLong("t_o_id", 32) ;

        // Simple Field (connection_serial_number)
int connection_serial_number = readBuffer.readUnsignedInt("connection_serial_number", 16) ;
        // Const Field (vendor_id)
        int vendor_id = readBuffer.readUnsignedInt("vendor_id", 16);
        if(vendor_id != LargeForwardOpenRequest.VENDOR_ID) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.VENDOR_ID + " but got " + vendor_id);
        }


        // Simple Field (originator_serial_number)
long originator_serial_number = readBuffer.readUnsignedLong("originator_serial_number", 32) ;
        // Const Field (trt)
        short trt = readBuffer.readUnsignedShort("trt", 8);
        if(trt != LargeForwardOpenRequest.TRT) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.TRT + " but got " + trt);
        }

        // Reserved Field (Compartmentalized so the "reserved" variable can't leak)
        {
            int reserved = readBuffer.readUnsignedInt("reserved", 16);
            if(reserved != (int) 0x00) {
                LOGGER.info("Expected constant value " + 0x00 + " but got " + reserved + " for reserved field.");
            }
        }

        // Reserved Field (Compartmentalized so the "reserved" variable can't leak)
        {
            short reserved = readBuffer.readUnsignedShort("reserved", 8);
            if(reserved != (short) 0x00) {
                LOGGER.info("Expected constant value " + 0x00 + " but got " + reserved + " for reserved field.");
            }
        }

        // Const Field (otrpi)
        long otrpi = readBuffer.readUnsignedLong("otrpi", 32);
        if(otrpi != LargeForwardOpenRequest.OTRPI) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.OTRPI + " but got " + otrpi);
        }

        // Const Field (otnp)
        long otnp = readBuffer.readUnsignedLong("otnp", 32);
        if(otnp != LargeForwardOpenRequest.OTNP) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.OTNP + " but got " + otnp);
        }

        // Const Field (torpi)
        long torpi = readBuffer.readUnsignedLong("torpi", 32);
        if(torpi != LargeForwardOpenRequest.TORPI) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.TORPI + " but got " + torpi);
        }

        // Const Field (tonp)
        long tonp = readBuffer.readUnsignedLong("tonp", 32);
        if(tonp != LargeForwardOpenRequest.TONP) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.TONP + " but got " + tonp);
        }

        // Const Field (transport_type)
        short transport_type = readBuffer.readUnsignedShort("transport_type", 8);
        if(transport_type != LargeForwardOpenRequest.TRANSPORT_TYPE) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.TRANSPORT_TYPE + " but got " + transport_type);
        }

        // Const Field (connection_path_size)
        short connection_path_size = readBuffer.readUnsignedShort("connection_path_size", 8);
        if(connection_path_size != LargeForwardOpenRequest.CONNECTION_PATH_SIZE) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.CONNECTION_PATH_SIZE + " but got " + connection_path_size);
        }

        // Const Field (connection_path)
        long connection_path = readBuffer.readUnsignedLong("connection_path", 32);
        if(connection_path != LargeForwardOpenRequest.CONNECTION_PATH) {
            throw new ParseException("Expected constant value " + LargeForwardOpenRequest.CONNECTION_PATH + " but got " + connection_path);
        }

        readBuffer.closeContext("LargeForwardOpenRequest");
        // Create the instance
        return new LargeForwardOpenRequestBuilder(o_t_id, t_o_id, connection_serial_number, originator_serial_number);
    }

    public static void staticSerialize(WriteBuffer writeBuffer, LargeForwardOpenRequest _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("LargeForwardOpenRequest");

        // Const Field (request_path_size)
        writeBuffer.writeUnsignedShort("request_path_size", 8, ((Number) 0x02).shortValue());

        // Const Field (request_path)
        writeBuffer.writeUnsignedLong("request_path", 32, ((Number) 0x01240620).longValue());

        // Const Field (ptt)
        writeBuffer.writeUnsignedShort("ptt", 8, ((Number) 0x06).shortValue());

        // Const Field (tot)
        writeBuffer.writeUnsignedShort("tot", 8, ((Number) 0x9c).shortValue());

        // Simple Field (o_t_id)
        long o_t_id = (long) _value.getO_t_id();
        writeBuffer.writeUnsignedLong("o_t_id", 32, ((Number) (o_t_id)).longValue());

        // Simple Field (t_o_id)
        long t_o_id = (long) _value.getT_o_id();
        writeBuffer.writeUnsignedLong("t_o_id", 32, ((Number) (t_o_id)).longValue());

        // Simple Field (connection_serial_number)
        int connection_serial_number = (int) _value.getConnection_serial_number();
        writeBuffer.writeUnsignedInt("connection_serial_number", 16, ((Number) (connection_serial_number)).intValue());

        // Const Field (vendor_id)
        writeBuffer.writeUnsignedInt("vendor_id", 16, ((Number) 0x002f).intValue());

        // Simple Field (originator_serial_number)
        long originator_serial_number = (long) _value.getOriginator_serial_number();
        writeBuffer.writeUnsignedLong("originator_serial_number", 32, ((Number) (originator_serial_number)).longValue());

        // Const Field (trt)
        writeBuffer.writeUnsignedShort("trt", 8, ((Number) 0x00).shortValue());

        // Reserved Field (reserved)
        writeBuffer.writeUnsignedInt("reserved", 16, ((Number) (int) 0x00).intValue());

        // Reserved Field (reserved)
        writeBuffer.writeUnsignedShort("reserved", 8, ((Number) (short) 0x00).shortValue());

        // Const Field (otrpi)
        writeBuffer.writeUnsignedLong("otrpi", 32, ((Number) 0x00002710).longValue());

        // Const Field (otnp)
        writeBuffer.writeUnsignedLong("otnp", 32, ((Number) 0x420007cc).longValue());

        // Const Field (torpi)
        writeBuffer.writeUnsignedLong("torpi", 32, ((Number) 0x00002710).longValue());

        // Const Field (tonp)
        writeBuffer.writeUnsignedLong("tonp", 32, ((Number) 0x420007cc).longValue());

        // Const Field (transport_type)
        writeBuffer.writeUnsignedShort("transport_type", 8, ((Number) 0xa3).shortValue());

        // Const Field (connection_path_size)
        writeBuffer.writeUnsignedShort("connection_path_size", 8, ((Number) 0x02).shortValue());

        // Const Field (connection_path)
        writeBuffer.writeUnsignedLong("connection_path", 32, ((Number) 0x01240220).longValue());
        writeBuffer.popContext("LargeForwardOpenRequest");
    }

    public static class LargeForwardOpenRequestBuilder implements CipServiceIO.CipServiceBuilder {
        private final long o_t_id;
        private final long t_o_id;
        private final int connection_serial_number;
        private final long originator_serial_number;

        public LargeForwardOpenRequestBuilder(long o_t_id, long t_o_id, int connection_serial_number, long originator_serial_number) {
            this.o_t_id = o_t_id;
            this.t_o_id = t_o_id;
            this.connection_serial_number = connection_serial_number;
            this.originator_serial_number = originator_serial_number;
        }

        public LargeForwardOpenRequest build() {
            return new LargeForwardOpenRequest(o_t_id, t_o_id, connection_serial_number, originator_serial_number);
        }
    }

}
