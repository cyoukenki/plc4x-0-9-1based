/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * "License"); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *   http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing,
 * software distributed under the License is distributed on an
 * "AS IS" BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
 * KIND, either express or implied.  See the License for the
 * specific language governing permissions and limitations
 * under the License.
 */
package org.apache.plc4x.java.eip.readwrite.io;

import static org.apache.plc4x.java.spi.generation.StaticHelper.*;

import org.apache.plc4x.java.eip.readwrite.*;
import org.apache.plc4x.java.eip.readwrite.io.*;
import org.apache.plc4x.java.eip.readwrite.types.*;
import org.apache.plc4x.java.api.exceptions.PlcRuntimeException;
import org.apache.plc4x.java.spi.generation.*;
import org.apache.plc4x.java.api.value.PlcValue;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

import java.lang.reflect.InvocationTargetException;
import java.lang.reflect.Method;
import java.math.BigInteger;
import java.time.*;
import java.util.LinkedList;
import java.util.List;
import java.util.function.Supplier;

// Code generated by code-generation. DO NOT EDIT.

public class CipServiceIO implements MessageIO<CipService, CipService> {

    private static final Logger LOGGER = LoggerFactory.getLogger(CipServiceIO.class);

    @Override
    public CipService parse(ReadBuffer readBuffer, Object... args) throws ParseException {
        if((args == null) || (args.length != 1)) {
            throw new PlcRuntimeException("Wrong number of arguments, expected 1, but got " + args.length);
        }
        Integer serviceLen;
        if(args[0] instanceof Integer) {
            serviceLen = (Integer) args[0];
        } else if (args[0] instanceof String) {
            serviceLen = Integer.valueOf((String) args[0]);
        } else {
            throw new PlcRuntimeException("Argument 0 expected to be of type Integer or a string which is parseable but was " + args[0].getClass().getName());
        }
        return CipServiceIO.staticParse(readBuffer, serviceLen);
    }

    @Override
    public void serialize(WriteBuffer writeBuffer, CipService value, Object... args) throws ParseException {
        CipServiceIO.staticSerialize(writeBuffer, value);
    }

    public static CipService staticParse(ReadBuffer readBuffer, Integer serviceLen) throws ParseException {
        readBuffer.pullContext("CipService");
        int startPos = readBuffer.getPos();
        int curPos;

        // Discriminator Field (service) (Used as input to a switch field)
        short service = readBuffer.readUnsignedShort("service", 8);


        // Switch Field (Depending on the discriminator values, passes the instantiation to a sub-type)
        CipServiceBuilder builder = null;
                if(EvaluationHelper.equals(service, 0x4C)) {
            builder = CipReadRequestIO.staticParse(readBuffer);
        } else 
                if(EvaluationHelper.equals(service, 0xCC)) {
            builder = CipReadResponseIO.staticParse(readBuffer, serviceLen);
        } else 
                if(EvaluationHelper.equals(service, 0x4D)) {
            builder = CipWriteRequestIO.staticParse(readBuffer);
        } else 
                if(EvaluationHelper.equals(service, 0xCD)) {
            builder = CipWriteResponseIO.staticParse(readBuffer);
        } else 
                if(EvaluationHelper.equals(service, 0x0A)) {
            builder = MultipleServiceRequestIO.staticParse(readBuffer, serviceLen);
        } else 
                if(EvaluationHelper.equals(service, 0x8A)) {
            builder = MultipleServiceResponseIO.staticParse(readBuffer, serviceLen);
        } else 
                if(EvaluationHelper.equals(service, 0x52)) {
            builder = CipUnconnectedRequestIO.staticParse(readBuffer);
        } else 
                {
            builder = DefaultUnKnownStreamFunctionIO.staticParse(readBuffer);
        }
        if (builder == null) {
            throw new ParseException("Unsupported case for discriminated type");
        }

        readBuffer.closeContext("CipService");
        // Create the instance
        return builder.build();
    }

    public static void staticSerialize(WriteBuffer writeBuffer, CipService _value) throws ParseException {
        int startPos = writeBuffer.getPos();
        writeBuffer.pushContext("CipService");

        // Discriminator Field (service) (Used as input to a switch field)
        short service = (short) _value.getService();
            writeBuffer.writeUnsignedShort("service", 8, ((Number) (service)).shortValue());

        // Switch field (Depending on the discriminator values, passes the instantiation to a sub-type)
        if(_value instanceof CipReadRequest) {
            CipReadRequestIO.staticSerialize(writeBuffer, (CipReadRequest) _value);
        } else 
        if(_value instanceof CipReadResponse) {
            CipReadResponseIO.staticSerialize(writeBuffer, (CipReadResponse) _value);
        } else 
        if(_value instanceof CipWriteRequest) {
            CipWriteRequestIO.staticSerialize(writeBuffer, (CipWriteRequest) _value);
        } else 
        if(_value instanceof CipWriteResponse) {
            CipWriteResponseIO.staticSerialize(writeBuffer, (CipWriteResponse) _value);
        } else 
        if(_value instanceof MultipleServiceRequest) {
            MultipleServiceRequestIO.staticSerialize(writeBuffer, (MultipleServiceRequest) _value);
        } else 
        if(_value instanceof MultipleServiceResponse) {
            MultipleServiceResponseIO.staticSerialize(writeBuffer, (MultipleServiceResponse) _value);
        } else 
        if(_value instanceof CipUnconnectedRequest) {
            CipUnconnectedRequestIO.staticSerialize(writeBuffer, (CipUnconnectedRequest) _value);
        } else 
        if(_value instanceof DefaultUnKnownStreamFunction) {
            DefaultUnKnownStreamFunctionIO.staticSerialize(writeBuffer, (DefaultUnKnownStreamFunction) _value);
        }
        writeBuffer.popContext("CipService");
    }

    public static interface CipServiceBuilder {
        CipService build();
    }

}
